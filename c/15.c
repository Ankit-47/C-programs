#include <stdio.h>
int main()
{
    int i,a,sum=0;
    printf("Enter the natural number to find it sum:");
    scanf("%d",&a);
    for (i=1;i<=a;i++)
   sum=sum+i;
   printf("%d is the sum of natural number....",sum);
   getch();

}
//sum of n number input by user
